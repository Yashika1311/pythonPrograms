# class Parent:
#     def area(self, x):
#         return x * x

# class Child(Parent): 

#     def takevalue(self):
#         a = float(input("Enter the value: "))
#         self.x = a
#         print("The result is:", a)

# obj = Child()  
# obj.takevalue()
# print("Area of the square is", obj.area(obj.x))


class Parent:

    def area(self, x,y):
        if y==None
        return x * x

class Child(Parent): 

    def takevalue(self):
        
        a = float(input("Enter the value: "))
        self.x = a
        print("The result is:", a)
class child2(Parent):
    
    def getvalue(self):
        s==int(input("enter the no 1 for sqare and 2 for rectangle"))

obj = Child()  
obj.takevalue()
print("Area of the square is", obj.area(obj.x))